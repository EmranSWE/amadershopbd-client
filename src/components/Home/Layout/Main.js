import React from 'react';
import Footer from '../../shared/Footer';
import Navbar from '../../shared/Navbar';

const Main = () => {
    return (
        <div>
            <Navbar></Navbar>
            <Footer></Footer>
        </div>
    );
};

export default Main;